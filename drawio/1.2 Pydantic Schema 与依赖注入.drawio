<mxfile host="65bd71144e">
    <diagram id="5NRCFf6T-4gHLXbIaInp" name="第 1 页">
        <mxGraphModel dx="1847" dy="827" grid="1" gridSize="10" guides="1" tooltips="1" connect="1" arrows="1" fold="1" page="1" pageScale="1" pageWidth="690" pageHeight="980" math="0" shadow="0">
            <root>
                <mxCell id="0"/>
                <mxCell id="1" parent="0"/>
                <mxCell id="2" value="为什么需要 Schema？&#xa;&#xa;&#xa;  FastAPI 接口需要知道&quot;前端发来什么数据&quot;和&quot;返回什么数据&quot;。如果直接用 dict，就像收快递不检查包裹——你不知道里面装了什么、有没有破损。Pydantic Schema 就是数据契约，在数据进入业务逻辑前自动校验和解析。&#xa; &#xa;&#xa;  是什么？&#xa;&#xa;&#xa;  BaseModel 是 Pydantic 的核心类，继承它就定义了一个带类型标注的数据模型。Field(...) 补充元信息（描述、默认值、校验规则）。" style="text;whiteSpace=wrap;fontSize=14;fillColor=#f5f5f5;fontColor=#333333;strokeColor=#666666;" vertex="1" parent="1">
                    <mxGeometry x="-620" y="10" width="700" height="210" as="geometry"/>
                </mxCell>
                <mxCell id="3" value="@cache 是 Python 3.9+ 标准库装饰器，第一次调用后把返回值缓存起来，后续调用直接返回同一对象——实现单例模式。FastAPI 可以通过 Depends(get_query_service)&#xa;  把服务实例注入路由函数。" style="text;whiteSpace=wrap;fontSize=14;fillColor=#dae8fc;strokeColor=#6c8ebf;" vertex="1" parent="1">
                    <mxGeometry x="-620" y="280" width="720" height="60" as="geometry"/>
                </mxCell>
                <UserObject label="3. 双重检查锁单例（knowledge/utils/client/base.py）：对于 GPU 模型客户端（BGE-M3、Reranker），单靠 @cache&#xa;  不够——多线程并发初始化时会重复加载模型。BaseClientManager._get_or_create() 用 threading.Lock 实现经典的双重检查锁（DCL）：&#xa;&#xa;&#xa;  # 第一次检查（无锁，快速路径）&#xa;  instance = getattr(cls, attr_name, None)&#xa;  if instance is not None:&#xa;      return instance&#xa;&#xa;&#xa;  with lock:&#xa;      # 第二次检查（持锁，防并发重复创建）&#xa;      instance = getattr(cls, attr_name, None)&#xa;      if instance is not None:&#xa;          return instance&#xa;      instance = factory()&#xa;      setattr(cls, attr_name, instance)" link="3. 双重检查锁单例（knowledge/utils/client/base.py）：对于 GPU 模型客户端（BGE-M3、Reranker），单靠 @cache&#xa;  不够——多线程并发初始化时会重复加载模型。BaseClientManager._get_or_create() 用 threading.Lock 实现经典的双重检查锁（DCL）：&#xa;&#xa;&#xa;  # 第一次检查（无锁，快速路径）&#xa;  instance = getattr(cls, attr_name, None)&#xa;  if instance is not None:&#xa;      return instance&#xa;&#xa;&#xa;  with lock:&#xa;      # 第二次检查（持锁，防并发重复创建）&#xa;      instance = getattr(cls, attr_name, None)&#xa;      if instance is not None:&#xa;          return instance&#xa;      instance = factory()&#xa;      setattr(cls, attr_name, instance)" id="4">
                    <mxCell style="text;whiteSpace=wrap;fontSize=17;fillColor=#d5e8d4;strokeColor=#82b366;" vertex="1" parent="1">
                        <mxGeometry x="-630" y="380" width="910" height="400" as="geometry"/>
                    </mxCell>
                </UserObject>
            </root>
        </mxGraphModel>
    </diagram>
</mxfile>